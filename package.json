{
  "scripts": {
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "sass:write": "sass --no-source-map source/styles/index.scss public/styles/index.css --style compressed",
    "sass:watch": "sass --no-source-map --watch source/styles/index.scss:public/styles/index.css --style compressed",
    "stylelint:write": "stylelint --fix source/styles/index.scss"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "stylelint": "^16.1.0",
    "stylelint-config-standard-scss": "^13.0.0",
    "stylelint-order": "^6.0.4"
  }
}
